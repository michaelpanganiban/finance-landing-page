import { Colors } from '../enum/colors';

export const HeaderComponent= {
	styleOverrides: {
		root: {
			backgroundColor: Colors.PRIMARY,
			height: '5%',
    },
		divider: {
			color: 'white'
		}
	},
}