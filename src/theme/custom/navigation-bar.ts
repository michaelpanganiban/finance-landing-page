import { COLORS } from '../../enum/colors';

export const NavBarComponent = {
	styleOverrides: {
		root: {
			backgroundColor: COLORS.WHITE,
			position: 'sticky',
			elevation: 0,
			color: COLORS.BLACK
		},
	},
}