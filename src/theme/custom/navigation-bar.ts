import { WHITE, BLACK } from '../../enum/colors';

export const NavBarComponent = {
	styleOverrides: {
		root: {
			backgroundColor: WHITE,
			position: 'sticky',
			elevation: 0,
			color: BLACK
		},
	},
}